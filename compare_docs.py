with open('paper/main.tex', 'r') as f:
    current = f.read()

import sys
# The user essentially wants me to overwrite the paper/main.tex with the giant blob they just provided.
# I will do exactly that to ensure their desired state is perfectly captured.
